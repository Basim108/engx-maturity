{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "rule.schema.json",
  "title": "Rule",
  "description": "An engineering excellence rule or practice that some maturity levels might contain.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "ruleId": {
      "$ref": "types.schema.json#/$defs/uuid"
    },
    "statement": {
      "type": "string",
      "description": "Brief summary of a rule in imperative form for guidelines"
    },
    "question": {
      "type": "string",
      "description": "Brief summary of a rule in interrogative form for questionnaires/assessments"
    },
    "description": {
      "type": "string",
      "description": "Detailed explanation of the rule and its context"
    },
    "tags": {
      "type": "array",
      "minItems": 0,
      "uniqueItems": true,
      "items": {
        "type": "string",
        "description": "for better further search. For example, 'security', 'metrics', etc"
      }
    },
    "articles": {
      "type": "array",
      "minItems": 0,
      "items": {
        "$ref": "types.schema.json#/$defs/resourceUrl"
      }
    },
    "metrics": {
      "type": "array",
      "minItems": 0,
      "uniqueItems": true,
      "items": {
        "$ref": "types.schema.json#/$defs/uniqueId"
      }
    }
  },
  "required": ["ruleId", "question"]
}